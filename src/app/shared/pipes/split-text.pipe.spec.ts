import { SplitTextPipe } from './split-text.pipe';

describe('SplitTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitTextPipe();
    expect(pipe).toBeTruthy();
  });
});
